<!-- {{#each posts as |posts|}}




 <!-- Developers, why are we using data-id and not just id? -->
  <h1 data-id="{{posts.id}}"> <small>Title: {{posts.title}}</small></h1>
  <div>
    {{#if posts.body}}
      {{posts.description}}
    {{else}}
      No description.
    {{/if}}
  </div>
  <!-- post -->

{{/each}}
